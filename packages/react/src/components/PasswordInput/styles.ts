import { styled } from '../../styles';

export const Button = styled('button', {
  all: 'unset',
  position: 'absolute',
  right: '$spacing-2',
  bottom: '$spacing-2',

  bg: 'transparent',
  zIndex: 2,
});
