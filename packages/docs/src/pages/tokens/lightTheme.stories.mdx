import { Meta, Source } from '@storybook/addon-docs';
import { light } from '@skala/design-tokens';

import { ColorsGrid } from '../../components/ColorsGrid';

<Meta title="Foundations/Colors/Theme/Light" />

# Light Theme

<br />

These are color tokens to be used within light-theme. These tokens are already available in all components.

<Source
  language="css"
  format={false}
  code={`
  import { Box } from '@skala/design-tokens';

<Box css={{ color: '$surface-color-text-default' }} /> `} />
<ColorsGrid tokens={light} />
